#include<stdio.h>
int main(){
    int T,num1,num2;
    printf("Enter The Number Which Loop Will Be Work =  ");
    scanf("%d",&T);
    for(int i=0; i<T; i++){
        printf("Enter The Number = ");
        scanf("%d",&num1,&num2);
        for(int i=num1; i<=num2; i++){
            while(i!=0){
                if(num%i)
            }
        }
    }

    return 0;
}